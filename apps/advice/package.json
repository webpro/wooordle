{
  "name": "@wooordle/advice",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts --debug",
    "dev:client": "vite",
    "build": "podman build -f ../../Dockerfile -t wooordle-advice ../..",
    "build:client": "vite build",
    "type-check": "tsc",
    "deploy": "pnpm build:client && pnpm build && wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.31",
    "@hono/node-server": "^1.19.8",
    "@wooordle/ocr": "workspace:*",
    "hono": "^4.11.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260113.0",
    "@types/node": "^24.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "wrangler": "^4.58.0"
  }
}
